{
  "_generated": "DO NOT EDIT. Generated by scripts/resolve_schema.py from findings.schema.json. Edit the source schema, then run: make resolve-schemas",
  "description": "Shared JSON shape emitted by producer skills (review, standards, c4-reverse-engineer) and consumer skill reports (apply-review). The document-level 'source' field discriminates which finding subtype is used and which top-level fields are required.",
  "properties": {
    "applied": {
      "items": {
        "properties": {
          "commit_sha": {
            "description": "Commit SHA of the change produced by this finding. Required semantics: set when outcome is 'applied'; absent for skipped/failed/pending-commit. Constraints: pattern: \"^[0-9a-f]{7,40}$\".",
            "type": "string"
          },
          "detail": {
            "description": "Short free-text note (e.g. reason for skip/fail, or a human-readable summary).",
            "type": "string"
          },
          "error_kind": {
            "description": "Machine-readable error classification. Populated only when outcome is 'failed'; mirrors issue.kind so auditors can aggregate failures by cause.",
            "enum": [
              "permission_denied",
              "subagent_failure",
              "validation_failed",
              "tool_unavailable",
              "schema_rejected_input",
              "other"
            ]
          },
          "finding_id": {
            "description": "Finding ID from the consumed Findings-*.json, e.g. C0, I3, S1. Constraints: pattern: \"^[CISN][0-9]+$\".",
            "type": "string"
          },
          "outcome": {
            "description": "applied = committed; skipped = no changes made; failed = attempted but errored; pending-commit = implemented in the working tree but left uncommitted (batch mode).",
            "enum": [
              "applied",
              "skipped",
              "failed",
              "pending-commit"
            ]
          }
        },
        "required": [
          "finding_id",
          "outcome"
        ],
        "type": "object",
        "unevaluatedProperties": false
      },
      "type": "array"
    },
    "decomposition": {
      "items": {
        "properties": {
          "dimension_name": {
            "description": "Constraints: minLength: 1.",
            "type": "string"
          },
          "dimension_scope": {
            "type": "object"
          },
          "dimension_slug": {
            "description": "Constraints: pattern: \"^[a-z0-9][a-z0-9-]{0,30}$\".",
            "type": "string"
          }
        },
        "required": [
          "dimension_name",
          "dimension_slug"
        ],
        "type": "object",
        "unevaluatedProperties": false
      },
      "type": "array"
    },
    "findings": {
      "type": "array"
    },
    "issues": {
      "items": {
        "properties": {
          "context": {
            "type": "object"
          },
          "kind": {
            "enum": [
              "permission_denied",
              "subagent_failure",
              "validation_failed",
              "tool_unavailable",
              "schema_rejected_input",
              "other"
            ]
          },
          "message": {
            "description": "Constraints: minLength: 1.",
            "type": "string"
          },
          "severity": {
            "enum": [
              "error",
              "warning"
            ]
          },
          "source_component": {
            "description": "Constraints: minLength: 1.",
            "type": "string"
          }
        },
        "required": [
          "severity",
          "kind",
          "message"
        ],
        "type": "object",
        "unevaluatedProperties": false
      },
      "type": "array"
    },
    "project": {
      "properties": {
        "name": {
          "description": "Constraints: minLength: 1.",
          "type": "string"
        },
        "scope_slug": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object",
      "unevaluatedProperties": false
    },
    "schema_version": {
      "description": "SemVer string, aligned with the plugin version at the time the document was written (.claude-plugin/plugin.json version). Consumers read this to identify which plugin release produced the envelope. Constraints: pattern: \"^(0|[1-9][0-9]*)\\\\.(0|[1-9][0-9]*)\\\\.(0|[1-9][0-9]*)(-((0|[1-9][0-9]*|[0-9]*[a-zA-Z-][0-9a-zA-Z-]*)(\\\\.(0|[1-9][0-9]*|[0-9]*[a-zA-Z-][0-9a-zA-Z-]*))*))?(\\\\+([0-9a-zA-Z-]+(\\\\.[0-9a-zA-Z-]+)*))?$\".",
      "type": "string"
    },
    "source": {
      "description": "The producing skill, or the shape of the output for consumer skills. `update-pr` is absent deliberately \u2014 it emits review-shaped findings (with optional `pr_comment` fields) under `source: \"review\"`.",
      "enum": [
        "review",
        "standards",
        "c4-reverse-engineer",
        "apply-review"
      ]
    },
    "supplementary": {
      "description": "Optional source-specific metadata that the render script uses to produce supplementary markdown. Not part of the cross-skill handoff contract; consumers may ignore it. Shape is defined by each producer skill.",
      "type": "object"
    }
  },
  "required": [
    "schema_version",
    "source",
    "project",
    "findings",
    "issues"
  ],
  "title": "Findings (cross-skill handoff)",
  "type": "object",
  "unevaluatedProperties": false
}
