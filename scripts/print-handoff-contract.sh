#!/usr/bin/env bash
# Generated By: Claude Code (Claude Opus 4.7)
# Print the shared handoff contract to stdout.
#
# Exists so SKILL.md files can inject the contract via `!` without
# triggering Claude Code's session-level `cat` permission check, which
# blocks reads of files outside the consuming project's working directory
# (the plugin root is outside every consuming project).

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cat "$SCRIPT_DIR/../resources/handoff-contract.md"
