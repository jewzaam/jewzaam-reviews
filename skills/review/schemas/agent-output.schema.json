{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jewzaam/jewzaam-reviews/skills/review/schemas/agent-output.schema.json",
  "title": "Agent Output",
  "description": "JSON contract returned by a single review sub-agent for one (concern, dimension) cell.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "agent_id",
    "concern",
    "concern_slug",
    "dimension_name",
    "dimension_slug",
    "dimension_scope",
    "findings"
  ],
  "properties": {
    "agent_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier for the sub-agent instance (e.g. 'architecture/auth'). Used for logging and issues[].source_component attribution."
    },
    "concern": {
      "type": "string",
      "description": "Human-readable concern axis. One of the seven fixed axes the review skill dispatches per dimension.",
      "enum": [
        "Architecture & Design",
        "Implementation Quality",
        "Test Quality & Coverage",
        "Maintainability & Standards",
        "Security",
        "Documentation",
        "Observability"
      ]
    },
    "concern_slug": {
      "type": "string",
      "description": "Machine-readable concern axis (kebab-case). Must match the human `concern` value.",
      "enum": [
        "architecture",
        "implementation",
        "test",
        "maintainability",
        "security",
        "documentation",
        "observability"
      ]
    },
    "dimension_name": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable name of the dimension being reviewed (e.g. 'auth subsystem')."
    },
    "dimension_slug": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,30}$",
      "description": "Filesystem-safe dimension slug (lowercase kebab-case, <=30 chars). Used in raw/<concern_slug>-<dimension_slug>.json."
    },
    "dimension_scope": {
      "type": "object",
      "additionalProperties": true,
      "description": "Free-form scope descriptor passed from the main agent. Typical shape: {paths: [...]} and/or {theme: '...'}."
    },
    "findings": {
      "type": "array",
      "description": "Findings detected by this agent within its (concern, dimension) cell. May be empty.",
      "items": {"$ref": "#/$defs/finding"}
    },
    "cross_cutting_observations": {
      "type": "array",
      "description": "Informational notes about patterns or issues outside the agent's dimension scope. Not scored, not rendered as findings.",
      "items": {"type": "string"}
    }
  },
  "$defs": {
    "finding": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "title",
        "impact",
        "likelihood",
        "effort_to_fix",
        "confidence",
        "locations",
        "issue",
        "why_it_matters",
        "suggested_fix"
      ],
      "properties": {
        "title": {"type": "string", "minLength": 1, "maxLength": 120, "description": "Short human-readable title for the finding (<=120 chars)."},
        "impact": {"type": "integer", "minimum": 0, "maximum": 100, "description": "Blast radius if the issue manifests. 0-100 integer."},
        "likelihood": {"type": "integer", "minimum": 0, "maximum": 100, "description": "Probability the issue actually occurs in real use. 0-100 integer."},
        "effort_to_fix": {"type": "integer", "minimum": 0, "maximum": 100, "description": "Rough cost to remediate; lower = cheaper. 0-100 integer."},
        "confidence": {"type": "integer", "minimum": 0, "maximum": 100, "description": "Agent's certainty this is a real issue grounded in concrete evidence. 0-100 integer."},
        "locations": {
          "type": "array",
          "minItems": 1,
          "description": "File+line pointers that anchor the finding. At least one entry required; the first (or role='primary') entry is the canonical location used for grouping and rendering.",
          "items": {"$ref": "#/$defs/location"}
        },
        "issue": {"type": "string", "minLength": 1, "description": "1-3 sentence prose description of the problem."},
        "why_it_matters": {"type": "string", "minLength": 1, "description": "Prose grounding the finding in the project's own patterns, standards, or stated requirements."},
        "suggested_fix": {"type": "string", "minLength": 1, "description": "Prose describing the recommended change."}
      }
    },
    "location": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "line"],
      "properties": {
        "path": {"type": "string", "minLength": 1, "description": "Repo-relative file path (POSIX separators)."},
        "line": {"type": "string", "pattern": "^[0-9]+(-[0-9]+)?$", "description": "Line number or inclusive range (e.g. '12' or '12-20'). Stored as a string so ranges are allowed."},
        "role": {
          "type": "string",
          "description": "primary = canonical location for this finding; related = supporting evidence elsewhere; callsite = where the issue is invoked; requirement = spec/standards file:line that states the requirement being violated. Defaults to primary when absent.",
          "enum": ["primary", "related", "callsite", "requirement"]
        }
      }
    }
  }
}
