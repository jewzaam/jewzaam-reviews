{
  "_generated": "DO NOT EDIT. Generated by scripts/resolve_schema.py from selector-output.schema.json. Edit the source schema, then run: make resolve-schemas",
  "additionalProperties": false,
  "description": "Output of the lens selector agent: which review lenses should run for this scope, and an optional dimension decomposition for large scopes.",
  "properties": {
    "dimensions": {
      "description": "Optional decomposition for large scopes. Omit for small scopes \u2014 the orchestrator defaults to a single full-scope dimension.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "name": {
            "description": "Short human-readable dimension name, e.g. \"auth subsystem\".",
            "pattern": "^[\\s\\S]+$",
            "type": "string"
          },
          "scope": {
            "additionalProperties": false,
            "description": "Scope descriptor for one review dimension. Closed rather than free-form: OpenAI strict Structured Outputs rejects an open object, and only `shared_infrastructure` is read by name (orchestrator/prompts.py) \u2014 the rest is serialized whole into the lens prompt and the report. null means not set.",
            "properties": {
              "paths": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "Repo-relative paths this dimension covers."
              },
              "shared_infrastructure": {
                "anyOf": [
                  {
                    "type": "boolean"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "True when the dimension covers code shared across other dimensions; alters the lens prompt."
              },
              "theme": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "Short thematic description when the dimension is not path-scoped."
              }
            },
            "required": [
              "paths",
              "theme",
              "shared_infrastructure"
            ],
            "type": "object"
          },
          "slug": {
            "description": "Filesystem-safe slug (lowercase, hyphens, max 31 chars).",
            "pattern": "^[a-z0-9][a-z0-9-]{0,30}$",
            "type": "string"
          }
        },
        "required": [
          "name",
          "slug",
          "scope"
        ],
        "type": "object"
      },
      "maxItems": 3,
      "type": "array"
    },
    "lenses": {
      "description": "Selected lenses. Empty or invalid selection triggers the all-lenses fallback in the orchestrator.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "name": {
            "description": "Lens slug \u2014 must match the concern_slug enum in agent-output.schema.json.",
            "enum": [
              "architecture",
              "implementation",
              "test",
              "maintainability",
              "security",
              "compatibility",
              "documentation",
              "observability"
            ],
            "type": "string"
          },
          "rationale": {
            "description": "One line grounded in what the scope actually touches.",
            "pattern": "^[\\s\\S]+$",
            "type": "string"
          }
        },
        "required": [
          "name",
          "rationale"
        ],
        "type": "object"
      },
      "type": "array"
    }
  },
  "required": [
    "lenses",
    "dimensions"
  ],
  "title": "Review lens selector output",
  "type": "object"
}
