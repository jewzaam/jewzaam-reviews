{
  "_generated": "DO NOT EDIT. Generated by scripts/resolve_schema.py from agent-output-simple.schema.json. Edit the source schema, then run: make resolve-schemas",
  "additionalProperties": false,
  "description": "JSON contract returned by a single review sub-agent for one (concern, dimension) cell when the review runs with --scoring simple. Findings carry direct severity + confidence instead of the five-dimension categorical rubric.",
  "properties": {
    "agent_id": {
      "description": "Stable identifier for the sub-agent instance (e.g. 'implementation/full-scope').",
      "minLength": 1,
      "type": "string"
    },
    "concern": {
      "description": "Human-readable concern axis. Same enum as agent-output.schema.json.",
      "enum": [
        "Architecture and Design",
        "Implementation Quality",
        "Test Quality and Coverage",
        "Maintainability and Standards",
        "Security",
        "Documentation",
        "Observability"
      ],
      "type": "string"
    },
    "concern_slug": {
      "description": "Machine-readable concern axis (kebab-case). Must match the human `concern` value.",
      "enum": [
        "architecture",
        "implementation",
        "test",
        "maintainability",
        "security",
        "documentation",
        "observability"
      ],
      "type": "string"
    },
    "cross_cutting_observations": {
      "description": "Informational notes about patterns or issues outside the agent's dimension scope. Not scored, not rendered as findings.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "dimension_name": {
      "description": "Human-readable name of the dimension being reviewed.",
      "minLength": 1,
      "type": "string"
    },
    "dimension_scope": {
      "additionalProperties": true,
      "description": "Free-form scope descriptor passed from the orchestrator.",
      "type": "object"
    },
    "dimension_slug": {
      "description": "Filesystem-safe dimension slug (lowercase kebab-case, <=30 chars).",
      "pattern": "^[a-z0-9][a-z0-9-]{0,30}$",
      "type": "string"
    },
    "findings": {
      "description": "Findings detected by this agent within its (concern, dimension) cell. May be empty.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "confidence": {
            "description": "high = demonstrated from the code. medium = inferred with a plausible failure scenario. low = speculative. Low-confidence findings render into the needs-review bucket rather than being dropped.",
            "enum": [
              "high",
              "medium",
              "low"
            ],
            "type": "string"
          },
          "issue": {
            "description": "1-3 sentence prose description of the problem.",
            "minLength": 1,
            "type": "string"
          },
          "locations": {
            "description": "File+line pointers that anchor the finding. At least one entry required; the first (or role='primary') entry is the canonical location.",
            "items": {
              "additionalProperties": false,
              "properties": {
                "line": {
                  "description": "Line number or inclusive range (e.g. '12' or '12-20').",
                  "pattern": "^[0-9]+(-[0-9]+)?$",
                  "type": "string"
                },
                "path": {
                  "description": "Repo-relative file path (POSIX separators).",
                  "minLength": 1,
                  "type": "string"
                },
                "role": {
                  "description": "primary = canonical location; related = supporting evidence; callsite = where the issue is invoked; requirement = spec/standards reference. Defaults to primary when absent.",
                  "enum": [
                    "primary",
                    "related",
                    "callsite",
                    "requirement"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "path",
                "line"
              ],
              "type": "object"
            },
            "minItems": 1,
            "type": "array"
          },
          "severity": {
            "description": "critical = data loss, security breach, or crash in production paths. important = incorrect behavior or degraded operation. suggestion = everything else worth fixing.",
            "enum": [
              "critical",
              "important",
              "suggestion"
            ],
            "type": "string"
          },
          "suggested_fix": {
            "description": "Prose describing the recommended change.",
            "minLength": 1,
            "type": "string"
          },
          "title": {
            "description": "Short human-readable title for the finding (<=120 chars).",
            "maxLength": 120,
            "minLength": 1,
            "type": "string"
          },
          "why_it_matters": {
            "description": "Prose grounding the finding in the project's own patterns, standards, or stated requirements.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "title",
          "severity",
          "confidence",
          "locations",
          "issue",
          "why_it_matters",
          "suggested_fix"
        ],
        "type": "object"
      },
      "type": "array"
    }
  },
  "required": [
    "agent_id",
    "concern",
    "concern_slug",
    "dimension_name",
    "dimension_slug",
    "dimension_scope",
    "findings"
  ],
  "title": "Agent Output (simple scoring)",
  "type": "object"
}
